API_HOST=31-128-42-29.sslip.io
POSTGRES_DB=vmeste
POSTGRES_USER=vmeste
# Используйте только URL-safe случайное значение, например результат `openssl rand -hex 32`.
POSTGRES_PASSWORD=replace-with-a-random-64-character-hex-value
SESSION_TTL_DAYS=30
# Native Expo-клиент не требует CORS. Укажите browser origins через запятую только при их появлении.
CORS_ORIGINS=
